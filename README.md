Fork https://github.com/khiemnguyen712/timetagger-main 

From the directory of timetagger-main, run `docker compose up`

TimeTagger run on localhost:80 

Login with username `test` and password `test`

Then `docker compose up` this one

This app run on localhost:8000

This app reads the SQLite in a volume created by TimeTagger
